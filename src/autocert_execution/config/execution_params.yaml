# AutoCert Execution Node Parameters
#
# Contains ONLY execution-specific parameters.
#
# IMPORTANT — what is NOT here (and why):
#   planning_pipelines / pipeline_names → defined ONLY in moveit.launch.py.
#   robot_description / robot_description_semantic → injected by execution.launch.py
#     directly into the Node as parameters at launch time.
#   Keeping those here would create duplicate, potentially conflicting definitions.

/**:
  ros__parameters:

    # MoveIt2 planning group — must match SRDF exactly
    planning_group: arm

    # End-effector link — must match URDF link name exactly
    end_effector_link: tool0

    # Reference/base frame for all pose goals
    reference_frame: base_link

    # Planner selection
    planner_id: RRTConnect
    planning_time: 5.0
    planning_attempts: 3

    # Trajectory speed scaling (0.0 – 1.0)
    velocity_scaling: 0.3
    acceleration_scaling: 0.3

    # Goal tolerances
    goal_position_tolerance: 0.001      # metres  (~1 mm)
    goal_orientation_tolerance: 0.01    # radians (~0.57°)
    goal_joint_tolerance: 0.001         # radians (~0.057°)

    # Orientation constraint enforcement
    apply_orientation_constraints: true
    orientation_tolerance_x: 0.01      # radians
    orientation_tolerance_y: 0.01      # radians
    orientation_tolerance_z: 0.01      # radians